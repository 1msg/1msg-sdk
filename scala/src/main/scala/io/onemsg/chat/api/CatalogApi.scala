/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.chat.api

import io.onemsg.chat.model.CreateCommerce200Response
import io.onemsg.chat.model.CreateCommerceRequest
import io.onemsg.chat.model.ErrorResponse
import io.onemsg.chat.model.GetCommerce200ResponseInner
import io.onemsg.chat.core.JsonSupport._
import sttp.client3._
import sttp.model.Method

object CatalogApi {
  def apply(baseUrl: String = "https://api.1msg.io") = new CatalogApi(baseUrl)
}

class CatalogApi(baseUrl: String) {

  /**
   * Update catalog/cart commerce settings via the `params` object.  - `params.is_catalog_visible` — show catalog storefront icon (`true`) or hide it (`false`). - `params.is_cart_enabled` — enable cart (`true`) or disable it (`false`).  Blocked when the channel subscription limit is exceeded. Requires a commerce-capable channel (Cloud Functions `/commerceWAV2`). 
   * 
   * Expected answers:
   *   code 200 : CreateCommerce200Response (Commerce updated)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param createCommerceRequest 
   */
  def createCommerce(apiKeyQuery: String)(token: String, createCommerceRequest: CreateCommerceRequest
): Request[Either[ResponseException[String, Exception], CreateCommerce200Response], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/commerce?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(createCommerceRequest)
      .response(asJson[CreateCommerce200Response])

  /**
   * Returns catalog/cart commerce settings for the channel.  - `is_catalog_visible` — show catalog storefront icon (`true`) or hide it (`false`). - `is_cart_enabled` — enable cart (`true`) or disable it (`false`). 
   * 
   * Expected answers:
   *   code 200 : Seq[GetCommerce200ResponseInner] (Commerce settings)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   */
  def getCommerce(apiKeyQuery: String)(token: String
): Request[Either[ResponseException[String, Exception], Seq[GetCommerce200ResponseInner]], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/commerce?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .response(asJson[Seq[GetCommerce200ResponseInner]])

}
