/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.chat.api

import io.onemsg.chat.model.ConversationalAutomation
import io.onemsg.chat.model.ErrorResponse
import io.circe.Json
import io.onemsg.chat.core.JsonSupport._
import sttp.client3._
import sttp.model.Method

object ChannelApi {
  def apply(baseUrl: String = "https://api.1msg.io") = new ChannelApi(baseUrl)
}

class ChannelApi(baseUrl: String) {

  /**
   * Get WhatsApp conversational components for the channel (welcome message, ice-breaker prompts, and slash commands).  Proxies Meta/360dialog `GET /conversational_automation`.  When `enable_welcome_message` is true and a user opens chat for the first time, Meta delivers a webhook message with `type: request_welcome`. The inbound formatter exposes that as `type: \"request_welcome\"` and `meta.request_welcome: true` so your webhook can send a custom welcome reply. 
   * 
   * Expected answers:
   *   code 200 : ConversationalAutomation (Conversational automation settings)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   */
  def getConversationalAutomation(apiKeyQuery: String)(token: String
): Request[Either[ResponseException[String, Exception], ConversationalAutomation], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/conversationalAutomation?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .response(asJson[ConversationalAutomation])

  /**
   * Returns WhatsApp Business API client connection status.
   * 
   * Expected answers:
   *   code 200 : Map[String, io.circe.Json] (Channel status)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   */
  def getStatus(apiKeyQuery: String)(token: String
): Request[Either[ResponseException[String, Exception], Map[String, io.circe.Json]], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/status?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .response(asJson[Map[String, io.circe.Json]])

  /**
   * Update WhatsApp conversational components.  Allowed body fields (others are ignored): - `enable_welcome_message` (boolean) - `prompts` (string[], max 4, each ≤ 80 chars) - `commands` (`{ command_name, command_description }[]`)  Proxies Meta/360dialog `POST /conversational_automation`. 
   * 
   * Expected answers:
   *   code 200 : Map[String, io.circe.Json] (Updated settings from WABA, or legacy response.error wrapper on upstream failure. )
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param conversationalAutomation 
   */
  def setConversationalAutomation(apiKeyQuery: String)(token: String, conversationalAutomation: ConversationalAutomation
): Request[Either[ResponseException[String, Exception], Map[String, io.circe.Json]], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/conversationalAutomation?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(conversationalAutomation)
      .response(asJson[Map[String, io.circe.Json]])

}
