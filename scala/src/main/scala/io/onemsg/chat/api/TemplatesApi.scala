/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.chat.api

import io.onemsg.chat.model.AddTemplateRequest
import io.onemsg.chat.model.ErrorResponse
import io.onemsg.chat.model.ListTemplates200Response
import io.onemsg.chat.model.SendTemplateRequest
import io.circe.Json
import io.onemsg.chat.core.JsonSupport._
import sttp.client3._
import sttp.model.Method

object TemplatesApi {
  def apply(baseUrl: String = "https://api.1msg.io") = new TemplatesApi(baseUrl)
}

class TemplatesApi(baseUrl: String) {

  /**
   * Expected answers:
   *   code 200 : Map[String, io.circe.Json] (Template created)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param addTemplateRequest 
   */
  def addTemplate(apiKeyQuery: String)(token: String, addTemplateRequest: Option[AddTemplateRequest] = None
): Request[Either[ResponseException[String, Exception], Map[String, io.circe.Json]], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/addTemplate?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(addTemplateRequest)
      .response(asJson[Map[String, io.circe.Json]])

  /**
   * Expected answers:
   *   code 200 : ListTemplates200Response (OK)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   */
  def listTemplates(apiKeyQuery: String)(token: String
): Request[Either[ResponseException[String, Exception], ListTemplates200Response], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/templates?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .response(asJson[ListTemplates200Response])

  /**
   * Expected answers:
   *   code 200 : Map[String, io.circe.Json] (Template removed)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param requestBody 
   */
  def removeTemplate(apiKeyQuery: String)(token: String, requestBody: Map[String, io.circe.Json]
): Request[Either[ResponseException[String, Exception], Map[String, io.circe.Json]], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/removeTemplate?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(requestBody)
      .response(asJson[Map[String, io.circe.Json]])

  /**
   * Send a WhatsApp template message (works outside the 24-hour session window).  Supported `params` button `sub_type` values include: `url`, `quick_reply`, `copy_code` / `coupon_code`, `catalog`, `flow`, `limited_time_offer`, and **`order_details`** (WhatsApp Payments **India only** — requires an approved Utility template with an ORDER_DETAILS button).  For India order/invoice payments outside 24h, include a button component:  ```json {   \"type\": \"button\",   \"sub_type\": \"order_details\",   \"index\": 0,   \"parameters\": [{     \"type\": \"action\",     \"action\": { \"order_details\": { \"reference_id\": \"...\", \"currency\": \"INR\", \"order\": {} } }   }] } ```  Convenience wrapper with structured fields: `POST /sendOrderDetails`. 
   * 
   * Expected answers:
   *   code 200 : Map[String, io.circe.Json] (OK)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendTemplateRequest 
   */
  def sendTemplate(apiKeyQuery: String)(token: String, sendTemplateRequest: Option[SendTemplateRequest] = None
): Request[Either[ResponseException[String, Exception], Map[String, io.circe.Json]], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendTemplate?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendTemplateRequest)
      .response(asJson[Map[String, io.circe.Json]])

}
