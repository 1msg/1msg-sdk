/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.chat.api

import io.onemsg.chat.model.CreateReadMessage200Response
import io.onemsg.chat.model.CreateReadMessage401Response
import io.onemsg.chat.model.DeleteMediaLegacyRequest
import io.onemsg.chat.model.ErrorResponse
import io.onemsg.chat.model.GetMmLiteStatus200Response
import io.onemsg.chat.model.ListMessages200Response
import io.onemsg.chat.model.MessageSentResponse
import io.onemsg.chat.model.RetrieveMedia200Response
import io.onemsg.chat.model.SendAddressMessageRequest
import io.onemsg.chat.model.SendButtonRequest
import io.onemsg.chat.model.SendContactRequest
import io.onemsg.chat.model.SendCtaUrlRequest
import io.onemsg.chat.model.SendFlowRequestHeader
import io.onemsg.chat.model.SendListRequest
import io.onemsg.chat.model.SendLocationRequestRequest
import io.onemsg.chat.model.SendOrderDetailsRequest
import io.onemsg.chat.model.SendPaymentRequestRequest
import io.onemsg.chat.model.SendReactionRequest
import io.onemsg.chat.model.SendStickerRequest
import io.onemsg.chat.model.SuccessResponse
import io.circe.Json
import io.onemsg.chat.core.JsonSupport._
import sttp.client3._
import io.onemsg.chat.core.FormSupport
import sttp.model.Method

object MessagingApi {
  def apply(baseUrl: String = "https://api.1msg.io") = new MessagingApi(baseUrl)
}

class MessagingApi(baseUrl: String) {

  /**
   * Expected answers:
   *   code 200 : CreateReadMessage200Response (Server response example)
   *   code 401 : CreateReadMessage401Response (Invalid token)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param messageId 
   * @param msgId 
   * @param typingIndicator Show WhatsApp typing indicator (max 25s or until reply)
   */
  def createReadMessage(apiKeyQuery: String)(token: String, messageId: Option[String] = None, msgId: Option[String] = None, typingIndicator: Option[Boolean] = None
): Request[Either[ResponseException[String, Exception], CreateReadMessage200Response], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/readMessage?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/x-www-form-urlencoded")
      .body(FormSupport.fromMap(
        "messageId" -> messageId,
        "msgId" -> msgId,
        "typingIndicator" -> typingIndicator
      ))
      .response(asJson[CreateReadMessage200Response])

  /**
   * Expected answers:
   *   code 200 :  (OK)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param requestBody 
   */
  def createUploadMedia(apiKeyQuery: String)(token: String, requestBody: Map[String, io.circe.Json]
): Request[Either[ResponseException[String, Exception], Unit], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/uploadMedia?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(requestBody)
      .response(asString.mapWithMetadata(ResponseAs.deserializeRightWithError(_ => Right(()))))

  /**
   * Delete previously uploaded media by numeric `mediaId` (from `/uploadMedia`).  This is the canonical deletion endpoint and uses the REST `DELETE` verb on the media resource path. The older `POST /deleteMedia` is a deprecated alias. 
   * 
   * Expected answers:
   *   code 200 : SuccessResponse (Operation successful)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param mediaId Numeric WABA media id
   */
  def deleteMedia(apiKeyQuery: String)(token: String, mediaId: String
): Request[Either[ResponseException[String, Exception], SuccessResponse], Any] =
    basicRequest
      .method(Method.DELETE, uri"$baseUrl/media/${mediaId}?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .response(asJson[SuccessResponse])

  /**
   * **Deprecated.** Use `DELETE /media/{mediaId}` instead.  This POST alias is kept for backward compatibility with earlier integrations. New integrations should call `DELETE /media/{mediaId}`: 1msg follows REST conventions for resource deletion going forward (delete a resource with the `DELETE` verb on its resource path). 
   * 
   * Expected answers:
   *   code 200 : SuccessResponse (Operation successful)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param deleteMediaLegacyRequest 
   */
  def deleteMediaLegacy(apiKeyQuery: String)(token: String, deleteMediaLegacyRequest: DeleteMediaLegacyRequest
): Request[Either[ResponseException[String, Exception], SuccessResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/deleteMedia?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(deleteMediaLegacyRequest)
      .response(asJson[SuccessResponse])

  /**
   * Expected answers:
   *   code 200 : GetMmLiteStatus200Response (MM Lite status for the channel)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   */
  def getMmLiteStatus(apiKeyQuery: String)(token: String
): Request[Either[ResponseException[String, Exception], GetMmLiteStatus200Response], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/mmLiteStatus?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .response(asJson[GetMmLiteStatus200Response])

  /**
   * Expected answers:
   *   code 200 : ListMessages200Response (OK)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   */
  def listMessages(apiKeyQuery: String)(token: String
): Request[Either[ResponseException[String, Exception], ListMessages200Response], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/messages?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .response(asJson[ListMessages200Response])

  /**
   * Get WABA media URL and metadata by mediaId (from uploadMedia). The returned `url` is temporary and typically expires within ~5 minutes. 
   * 
   * Expected answers:
   *   code 200 : RetrieveMedia200Response (Media metadata)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param mediaId 
   */
  def retrieveMedia(apiKeyQuery: String)(token: String, mediaId: String
): Request[Either[ResponseException[String, Exception], RetrieveMedia200Response], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/retrieveMedia?token=${ token }&mediaId=${ mediaId }&token=${apiKeyQuery}")
      .contentType("application/json")
      .response(asJson[RetrieveMedia200Response])

  /**
   * Request shipping address from the user.  **India only** (WhatsApp Cloud API address messages). Requires an India WhatsApp Business number and an India (+91) recipient. Meta validates eligibility; mismatches return WABA errors such as `Unsupported Interactive Message type` (HTTP 200 with `sent: false`).  The outbound payload always sends `action.parameters.country = \"IN\"`. A `country` field in the request body (if present) is ignored. 
   * 
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendAddressMessageRequest 
   */
  def sendAddressMessage(apiKeyQuery: String)(token: String, sendAddressMessageRequest: SendAddressMessageRequest
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendAddressMessage?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendAddressMessageRequest)
      .response(asJson[MessageSentResponse])

  /**
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendButtonRequest 
   */
  def sendButton(apiKeyQuery: String)(token: String, sendButtonRequest: Option[SendButtonRequest] = None
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendButton?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendButtonRequest)
      .response(asJson[MessageSentResponse])

  /**
   * You can send product cards via Carousel in two ways:  Template messages: do not require a 24-hour customer service window between you and the recipient. Use sendTemplate.  Free-form messages: can be sent only when a customer service window is open between you and the recipient. Use sendCarousel.  The message structure in /sendCarousel is largely similar to sending a template. However, in this case you must  explicitly specify all elements that are created in advance when working with templates. This is because the message  is sent without using a template.  In /sendCarousel, for sending a Catalog Carousel there can be either 1 URL button or one or more quick reply buttons. 
   * 
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param params Required. Template-like structure (same as sendTemplate params). Must include a CAROUSEL component and its cards.  Structure: - params[] item with type CAROUSEL and cards (2..10) - cards[] item:   - components[] items:     - header: parameters[0] with type image/video/product       - image: {\\\"image\\\": {\\\"link\\\": \\\"https://...\\\"}}       - video: {\\\"video\\\": {\\\"link\\\": \\\"https://...\\\"}}       - product: {\\\"product\\\": {\\\"catalog_id\\\": \\\"...\\\", \\\"product_retailer_id\\\": \\\"...\\\"}}     - button: sub_type url or quick_reply       - url: parameters {\\\"text\\\": \\\"Button\\\", \\\"url\\\": \\\"https://...\\\"}       - quick_reply: parameters[] of {\\\"id\\\": \\\"...\\\", \\\"text\\\": \\\"...\\\"} 
   * @param body Text shown above the carousel. Optional. If omitted and params include a body component, the body will be taken from params.
   * @param quotedMsgId Quoted message ID (Cloud API)
   * @param chatId Required if phone is not set. Chat ID from the message list. Examples: 12020721369@c.us or 120363046942338209@g.us(group)
   * @param phone Required if chatId is not set. A phone number starting with the country code. USA example: 12020721369.
   */
  def sendCarousel(apiKeyQuery: String)(token: String, params: Seq[io.circe.Json], body: Option[String] = None, quotedMsgId: Option[String] = None, chatId: Option[String] = None, phone: Option[Int] = None
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendCarousel?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/x-www-form-urlencoded")
      .body(FormSupport.fromMap(
        "body" -> body,
        "params" -> FormSupport.jsonSeq(params),
        "quotedMsgId" -> quotedMsgId,
        "chatId" -> chatId,
        "phone" -> phone
      ))
      .response(asJson[MessageSentResponse])

  /**
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendContactRequest 
   */
  def sendContact(apiKeyQuery: String)(token: String, sendContactRequest: Option[SendContactRequest] = None
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendContact?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendContactRequest)
      .response(asJson[MessageSentResponse])

  /**
   * Send an interactive message with a single call-to-action URL button.
   * 
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendCtaUrlRequest 
   */
  def sendCtaUrl(apiKeyQuery: String)(token: String, sendCtaUrlRequest: SendCtaUrlRequest
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendCtaUrl?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendCtaUrlRequest)
      .response(asJson[MessageSentResponse])

  /**
   * Send a file to an existing chat. (Only if the dialogue has an Open Session).  Only one of two parameters is needed to determine the destination - chatId or phone. 
   * 
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param body File source (required unless mediaId is set): - HTTP/HTTPS URL - Base64 data URI 
   * @param filename File name with extension (required with body, not with mediaId)
   * @param mediaId WABA media id from uploadMedia (alternative to body)
   * @param mediaType Required when using mediaId
   * @param voice Native WhatsApp voice note (audio/ogg only)
   * @param caption 
   * @param quotedMsgId 
   * @param chatId 
   * @param phone 
   */
  def sendFile(apiKeyQuery: String)(token: String, body: Option[String] = None, filename: Option[String] = None, mediaId: Option[String] = None, mediaType: Option[String] = None, voice: Option[Boolean] = None, caption: Option[String] = None, quotedMsgId: Option[String] = None, chatId: Option[String] = None, phone: Option[Int] = None
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendFile?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/x-www-form-urlencoded")
      .body(FormSupport.fromMap(
        "body" -> body,
        "filename" -> filename,
        "mediaId" -> mediaId,
        "mediaType" -> mediaType,
        "voice" -> voice,
        "caption" -> caption,
        "quotedMsgId" -> quotedMsgId,
        "chatId" -> chatId,
        "phone" -> phone
      ))
      .response(asJson[MessageSentResponse])

  /**
   * Send Interactive WhatsApp Flow message to an existing chat. (Only if the dialogue has an Open Session).  Only one of two parameters is needed to determine the destination - chatId or phone.  Use this method to send a published WhatsApp Flow as a service (interactive) message.  If the 24-hour window is closed, send a template with a FLOW button via /sendTemplate. 
   * 
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param body Flow message body text
   * @param flowId Published Flow ID
   * @param flowToken Flow token generated by the business
   * @param flowCta CTA button text
   * @param header 
   * @param footer Footer text
   * @param flowAction Flow action type
   * @param flowActionPayload Required for flowAction=navigate (screen is required). Ignored for data_exchange. If data is provided, it must be a non-empty object.
   * @param flowMessageVersion Flow message version (default \\\"3\\\")
   * @param mode Flow mode (draft or published). If omitted, provider default applies
   * @param flowActionData Shortcut for flowActionPayload.data (optional)
   * @param flowActionScreen Shortcut for flowActionPayload.screen (optional)
   * @param quotedMsgId Quoted message ID (Cloud API)
   * @param chatId Required if phone is not set. Chat ID from the message list. Examples: 12020721369@c.us or 120363046942338209@g.us(group)
   * @param phone Required if chatId is not set. A phone number starting with the country code. USA example: 12020721369.
   */
  def sendFlow(apiKeyQuery: String)(token: String, body: String, flowId: String, flowToken: String, flowCta: String, header: Option[SendFlowRequestHeader] = None, footer: Option[String] = None, flowAction: Option[String] = None, flowActionPayload: Option[io.circe.Json] = None, flowMessageVersion: Option[String] = None, mode: Option[String] = None, flowActionData: Option[io.circe.Json] = None, flowActionScreen: Option[String] = None, quotedMsgId: Option[String] = None, chatId: Option[String] = None, phone: Option[Int] = None
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendFlow?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/x-www-form-urlencoded")
      .body(FormSupport.fromMap(
        "body" -> body,
        "header" -> header,
        "footer" -> footer,
        "flowId" -> flowId,
        "flowToken" -> flowToken,
        "flowCta" -> flowCta,
        "flowAction" -> flowAction,
        "flowActionPayload" -> flowActionPayload,
        "flowMessageVersion" -> flowMessageVersion,
        "mode" -> mode,
        "flowActionData" -> flowActionData,
        "flowActionScreen" -> flowActionScreen,
        "quotedMsgId" -> quotedMsgId,
        "chatId" -> chatId,
        "phone" -> phone
      ))
      .response(asJson[MessageSentResponse])

  /**
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendListRequest 
   */
  def sendList(apiKeyQuery: String)(token: String, sendListRequest: Option[SendListRequest] = None
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendList?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendListRequest)
      .response(asJson[MessageSentResponse])

  /**
   * Send a location to an existing chat. (Only if the dialogue has an Open Session).  Only one of two parameters is needed to determine the destination - chatId or phone. 
   * 
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param lat Latitude of the location. Example: 45.018337
   * @param lng Longitude of the location. Example: -73.968285
   * @param address Address of the location. Only displayed if name is present. Example: 9766 Valley View St., New York, NY 10024
   * @param name Name of the location. Example: Facebook HQ
   * @param quotedMsgId Quoted message ID (Cloud API)
   * @param chatId Required if phone is not set. Chat ID from the message list. Examples: 12020721369@c.us or 120363046942338209@g.us(group)
   * @param phone Required if chatId is not set. A phone number starting with the country code. USA example: 12020721369.
   */
  def sendLocation(apiKeyQuery: String)(token: String, lat: String, lng: String, address: Option[String] = None, name: Option[String] = None, quotedMsgId: Option[String] = None, chatId: Option[String] = None, phone: Option[Int] = None
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendLocation?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/x-www-form-urlencoded")
      .body(FormSupport.fromMap(
        "lat" -> lat,
        "lng" -> lng,
        "address" -> address,
        "name" -> name,
        "quotedMsgId" -> quotedMsgId,
        "chatId" -> chatId,
        "phone" -> phone
      ))
      .response(asJson[MessageSentResponse])

  /**
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendLocationRequestRequest 
   */
  def sendLocationRequest(apiKeyQuery: String)(token: String, sendLocationRequestRequest: Option[SendLocationRequestRequest] = None
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendLocationRequest?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendLocationRequestRequest)
      .response(asJson[MessageSentResponse])

  /**
   * Send a message to an existing chat. (Only if the dialogue has an Open Session).  The message will be added to the queue for sending and delivered even if the phone  is disconnected from the Internet or authorization is not passed.  Only one of two parameters is needed to determine the destination - chatId or phone. 
   * 
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param body Message text, UTF-8 or UTF-16 string with emoji
   * @param quotedMsgId Message ID to quote/reply to (Cloud API wamid)
   * @param chatId Chat ID in format: phone@c.us (individual) or phone@g.us (group)
   * @param phone Phone number starting with country code (alternative to chatId)
   */
  def sendMessage(apiKeyQuery: String)(token: String, body: String, quotedMsgId: Option[String] = None, chatId: Option[String] = None, phone: Option[Int] = None
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendMessage?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/x-www-form-urlencoded")
      .body(FormSupport.fromMap(
        "body" -> body,
        "quotedMsgId" -> quotedMsgId,
        "chatId" -> chatId,
        "phone" -> phone
      ))
      .response(asJson[MessageSentResponse])

  /**
   * Send a WhatsApp **order details** payment message via a pre-approved **Utility** template with an `ORDER_DETAILS` button.  **India only** (WhatsApp Payments India). Requires: - India WhatsApp Business Account / phone number - Commerce-enabled channel - Approved template with an `ORDER_DETAILS` button  Prefer this helper when you want a dedicated payload (`order`, `referenceId`, `currency`, `paymentSettings`). Under the hood it builds a Cloud API template `button` component with `sub_type: order_details` and calls the same path as `POST /sendTemplate`.  To send the same message **outside the 24-hour window**, you can also call `POST /sendTemplate` directly with a `params` button:  ```json {   \"type\": \"button\",   \"sub_type\": \"order_details\",   \"index\": 0,   \"parameters\": [{     \"type\": \"action\",     \"action\": {       \"order_details\": {         \"reference_id\": \"order-123\",         \"type\": \"digital-goods\",         \"payment_type\": \"upi\",         \"payment_configuration\": \"payment_config_name\",         \"currency\": \"INR\",         \"total_amount\": { \"offset\": 100, \"value\": 65000 },         \"order\": { \"status\": \"pending\", \"items\": [], \"subtotal\": { \"offset\": 100, \"value\": 65000 } }       }     }   }] } ```  See Meta/360dialog: Payments India — order details template message. 
   * 
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendOrderDetailsRequest 
   */
  def sendOrderDetails(apiKeyQuery: String)(token: String, sendOrderDetailsRequest: SendOrderDetailsRequest
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendOrderDetails?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendOrderDetailsRequest)
      .response(asJson[MessageSentResponse])

  /**
   * Send a regional payment request interactive message (beta scaffold). `region` must be IN, SG, or BR. Payload shape follows Meta regional payments docs; verify on stage before production use. Full regional builders are not implemented yet. 
   * 
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendPaymentRequestRequest 
   */
  def sendPaymentRequest(apiKeyQuery: String)(token: String, sendPaymentRequestRequest: SendPaymentRequestRequest
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendPaymentRequest?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendPaymentRequestRequest)
      .response(asJson[MessageSentResponse])

  /**
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param requestBody 
   */
  def sendProduct(apiKeyQuery: String)(token: String, requestBody: Map[String, io.circe.Json]
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendProduct?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(requestBody)
      .response(asJson[MessageSentResponse])

  /**
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendReactionRequest 
   */
  def sendReaction(apiKeyQuery: String)(token: String, sendReactionRequest: Option[SendReactionRequest] = None
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendReaction?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendReactionRequest)
      .response(asJson[MessageSentResponse])

  /**
   * Send a WhatsApp sticker by mediaId or link URL.
   * 
   * Expected answers:
   *   code 200 : MessageSentResponse (Message sent successfully)
   *   code 400 : ErrorResponse (Invalid request parameters)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 429 : ErrorResponse (Rate limit exceeded)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param sendStickerRequest 
   */
  def sendSticker(apiKeyQuery: String)(token: String, sendStickerRequest: SendStickerRequest
): Request[Either[ResponseException[String, Exception], MessageSentResponse], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/sendSticker?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(sendStickerRequest)
      .response(asJson[MessageSentResponse])

}
