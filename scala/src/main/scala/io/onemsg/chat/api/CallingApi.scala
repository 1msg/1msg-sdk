/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.chat.api

import io.onemsg.chat.model.ErrorResponse
import io.circe.Json
import io.onemsg.chat.core.JsonSupport._
import sttp.client3._
import sttp.model.Method

object CallingApi {
  def apply(baseUrl: String = "https://api.1msg.io") = new CallingApi(baseUrl)
}

class CallingApi(baseUrl: String) {

  /**
   * WhatsApp Calling API settings (beta). Requires Meta Calling enablement on the WABA. Not production-complete — paths and webhook field names may change. Trial/subscription-limited channels are blocked. 
   * 
   * Expected answers:
   *   code 200 : Map[String, io.circe.Json] (Calling settings)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   */
  def getCallingSettings(apiKeyQuery: String)(token: String
): Request[Either[ResponseException[String, Exception], Map[String, io.circe.Json]], Any] =
    basicRequest
      .method(Method.GET, uri"$baseUrl/callingSettings?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .response(asJson[Map[String, io.circe.Json]])

  /**
   * Outbound Calling API (beta). Requires Meta Calling enablement and product consent. Not production-complete — verify on stage before relying on this in production. Trial/subscription-limited channels are blocked. 
   * 
   * Expected answers:
   *   code 200 : Map[String, io.circe.Json] (Call initiated)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param requestBody 
   */
  def initiateCall(apiKeyQuery: String)(token: String, requestBody: Map[String, io.circe.Json]
): Request[Either[ResponseException[String, Exception], Map[String, io.circe.Json]], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/initiateCall?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(requestBody)
      .response(asJson[Map[String, io.circe.Json]])

  /**
   * Update WhatsApp Calling API settings (beta). Requires Meta Calling enablement. Trial/subscription-limited channels are blocked. 
   * 
   * Expected answers:
   *   code 200 : Map[String, io.circe.Json] (Updated)
   *   code 401 : ErrorResponse (Invalid or missing authentication token)
   *   code 500 : ErrorResponse (Internal server error)
   * 
   * Available security schemes:
   *   tokenAuth (apiKey)
   * 
   * @param token JWT token or API key for authorization
   * @param requestBody 
   */
  def updateCallingSettings(apiKeyQuery: String)(token: String, requestBody: Map[String, io.circe.Json]
): Request[Either[ResponseException[String, Exception], Map[String, io.circe.Json]], Any] =
    basicRequest
      .method(Method.POST, uri"$baseUrl/callingSettings?token=${ token }&token=${apiKeyQuery}")
      .contentType("application/json")
      .body(requestBody)
      .response(asJson[Map[String, io.circe.Json]])

}
