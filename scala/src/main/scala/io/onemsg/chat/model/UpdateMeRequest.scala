/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.chat.model

import io.circe.{Decoder, DecodingFailure, Encoder, Json}
import io.circe.syntax._
import io.onemsg.chat.core.JsonSupport._

case class UpdateMeRequest(
  /* Profile About section */
  about: Option[String] = None,
  address: Option[String] = None,
  /* Business description (max 256 characters) */
  description: Option[String] = None,
  email: Option[String] = None,
  /* Industry of the business */
  vertical: Option[String] = None,
  /* HTTPS URL or data:image/...;base64,... payload */
  photo: Option[String] = None,
  websites: Option[Seq[String]] = None
)
object UpdateMeRequest {
  implicit val encoderUpdateMeRequest: Encoder[UpdateMeRequest] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        t.about.map(v => "about" -> v.asJson),
        t.address.map(v => "address" -> v.asJson),
        t.description.map(v => "description" -> v.asJson),
        t.email.map(v => "email" -> v.asJson),
        t.vertical.map(v => "vertical" -> v.asJson),
        t.photo.map(v => "photo" -> v.asJson),
        t.websites.map(v => "websites" -> v.asJson)
      ).flatten
    }
  }
  implicit val decoderUpdateMeRequest: Decoder[UpdateMeRequest] = Decoder.instance { c =>
    for {
      about <- c.downField("about").as[Option[String]]
      address <- c.downField("address").as[Option[String]]
      description <- c.downField("description").as[Option[String]]
      email <- c.downField("email").as[Option[String]]
      vertical <- c.downField("vertical").as[Option[String]]
      photo <- c.downField("photo").as[Option[String]]
      websites <- c.downField("websites").as[Option[Seq[String]]]
    } yield UpdateMeRequest(
      about = about,
      address = address,
      description = description,
      email = email,
      vertical = vertical,
      photo = photo,
      websites = websites
    )
  }
}

