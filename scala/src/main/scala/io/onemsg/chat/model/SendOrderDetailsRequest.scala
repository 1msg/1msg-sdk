/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.chat.model

import io.circe.{Decoder, DecodingFailure, Encoder, Json}
import io.circe.syntax._
import io.onemsg.chat.core.JsonSupport._

case class SendOrderDetailsRequest(
  /* Recipient phone (India E.164 digits, no +) */
  phone: Option[Int] = None,
  /* Recipient chatId (phone@c.us / bsuid@lid / …) */
  chatId: Option[String] = None,
  /* Approved template name with ORDER_DETAILS button */
  template: String,
  /* Template namespace */
  namespace: String,
  /* Template language object, e.g. code \"en\" or policy \"deterministic\" with code \"en\".  */
  language: Map[String, io.circe.Json],
  /* Extra template components (header/body). An order_details button is appended automatically if missing.  */
  params: Option[Seq[Map[String, io.circe.Json]]] = None,
  /* Order object (items, subtotal, tax, shipping, discount, status) per Meta Payments India docs.  */
  order: Map[String, io.circe.Json],
  /* Unique order/payment reference id */
  referenceId: Option[String] = None,
  /* Optional payment_settings (UPI / payment gateway / payment link).  */
  paymentSettings: Option[Map[String, io.circe.Json]] = None,
  /* Currency code (INR for India) */
  currency: Option[String] = None
)
object SendOrderDetailsRequest {
  implicit val encoderSendOrderDetailsRequest: Encoder[SendOrderDetailsRequest] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        t.phone.map(v => "phone" -> v.asJson),
        t.chatId.map(v => "chatId" -> v.asJson),
        Some("template" -> t.template.asJson),
        Some("namespace" -> t.namespace.asJson),
        Some("language" -> t.language.asJson),
        t.params.map(v => "params" -> v.asJson),
        Some("order" -> t.order.asJson),
        t.referenceId.map(v => "referenceId" -> v.asJson),
        t.paymentSettings.map(v => "paymentSettings" -> v.asJson),
        t.currency.map(v => "currency" -> v.asJson)
      ).flatten
    }
  }
  implicit val decoderSendOrderDetailsRequest: Decoder[SendOrderDetailsRequest] = Decoder.instance { c =>
    for {
      phone <- c.downField("phone").as[Option[Int]]
      chatId <- c.downField("chatId").as[Option[String]]
      template <- c.downField("template").as[String]
      namespace <- c.downField("namespace").as[String]
      language <- c.downField("language").as[Map[String, io.circe.Json]]
      params <- c.downField("params").as[Option[Seq[Map[String, io.circe.Json]]]]
      order <- c.downField("order").as[Map[String, io.circe.Json]]
      referenceId <- c.downField("referenceId").as[Option[String]]
      paymentSettings <- c.downField("paymentSettings").as[Option[Map[String, io.circe.Json]]]
      currency <- c.downField("currency").as[Option[String]]
    } yield SendOrderDetailsRequest(
      phone = phone,
      chatId = chatId,
      template = template,
      namespace = namespace,
      language = language,
      params = params,
      order = order,
      referenceId = referenceId,
      paymentSettings = paymentSettings,
      currency = currency
    )
  }
}

