/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.chat.model

import java.net.URI
import io.circe.{Decoder, DecodingFailure, Encoder, Json}
import io.circe.syntax._
import io.onemsg.chat.core.JsonSupport._

case class SendCtaUrlRequest(
  phone: Option[Int] = None,
  chatId: Option[String] = None,
  body: String,
  /* Button label */
  displayText: String,
  url: URI,
  header: Option[Map[String, io.circe.Json]] = None,
  footer: Option[String] = None,
  quotedMsgId: Option[String] = None
)
object SendCtaUrlRequest {
  implicit val encoderSendCtaUrlRequest: Encoder[SendCtaUrlRequest] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        t.phone.map(v => "phone" -> v.asJson),
        t.chatId.map(v => "chatId" -> v.asJson),
        Some("body" -> t.body.asJson),
        Some("displayText" -> t.displayText.asJson),
        Some("url" -> t.url.asJson),
        t.header.map(v => "header" -> v.asJson),
        t.footer.map(v => "footer" -> v.asJson),
        t.quotedMsgId.map(v => "quotedMsgId" -> v.asJson)
      ).flatten
    }
  }
  implicit val decoderSendCtaUrlRequest: Decoder[SendCtaUrlRequest] = Decoder.instance { c =>
    for {
      phone <- c.downField("phone").as[Option[Int]]
      chatId <- c.downField("chatId").as[Option[String]]
      body <- c.downField("body").as[String]
      displayText <- c.downField("displayText").as[String]
      url <- c.downField("url").as[URI]
      header <- c.downField("header").as[Option[Map[String, io.circe.Json]]]
      footer <- c.downField("footer").as[Option[String]]
      quotedMsgId <- c.downField("quotedMsgId").as[Option[String]]
    } yield SendCtaUrlRequest(
      phone = phone,
      chatId = chatId,
      body = body,
      displayText = displayText,
      url = url,
      header = header,
      footer = footer,
      quotedMsgId = quotedMsgId
    )
  }
}

