/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.chat.model

import io.circe.{Decoder, DecodingFailure, Encoder, Json}
import io.circe.syntax._
import io.onemsg.chat.core.JsonSupport._

  /**
   * Send file by URL/base64 or by mediaId
   */
case class SendFileRequest(
  /* File source (required unless mediaId is set): - HTTP/HTTPS URL - Base64 data URI  */
  body: Option[String] = None,
  /* File name with extension (required with body, not with mediaId) */
  filename: Option[String] = None,
  /* WABA media id from uploadMedia (alternative to body) */
  mediaId: Option[String] = None,
  /* Required when using mediaId */
  mediaType: Option[SendFileRequestEnums.MediaType] = None,
  /* Native WhatsApp voice note (audio/ogg only) */
  voice: Option[Boolean] = None,
  caption: Option[String] = None,
  quotedMsgId: Option[String] = None,
  chatId: Option[String] = None,
  phone: Option[Int] = None
)
object SendFileRequest {
  implicit val encoderSendFileRequest: Encoder[SendFileRequest] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        t.body.map(v => "body" -> v.asJson),
        t.filename.map(v => "filename" -> v.asJson),
        t.mediaId.map(v => "mediaId" -> v.asJson),
        t.mediaType.map(v => "mediaType" -> v.asJson),
        t.voice.map(v => "voice" -> v.asJson),
        t.caption.map(v => "caption" -> v.asJson),
        t.quotedMsgId.map(v => "quotedMsgId" -> v.asJson),
        t.chatId.map(v => "chatId" -> v.asJson),
        t.phone.map(v => "phone" -> v.asJson)
      ).flatten
    }
  }
  implicit val decoderSendFileRequest: Decoder[SendFileRequest] = Decoder.instance { c =>
    for {
      body <- c.downField("body").as[Option[String]]
      filename <- c.downField("filename").as[Option[String]]
      mediaId <- c.downField("mediaId").as[Option[String]]
      mediaType <- c.downField("mediaType").as[Option[SendFileRequestEnums.MediaType]]
      voice <- c.downField("voice").as[Option[Boolean]]
      caption <- c.downField("caption").as[Option[String]]
      quotedMsgId <- c.downField("quotedMsgId").as[Option[String]]
      chatId <- c.downField("chatId").as[Option[String]]
      phone <- c.downField("phone").as[Option[Int]]
    } yield SendFileRequest(
      body = body,
      filename = filename,
      mediaId = mediaId,
      mediaType = mediaType,
      voice = voice,
      caption = caption,
      quotedMsgId = quotedMsgId,
      chatId = chatId,
      phone = phone
    )
  }
}

object SendFileRequestEnums {

  type MediaType = MediaType.Value
  object MediaType extends Enumeration {
    val Image = Value("image")
    val Video = Value("video")
    val Audio = Value("audio")
    val Document = Value("document")
  }

}
