/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.sdk.model

import io.circe.{Decoder, DecodingFailure, Encoder, Json}
import io.circe.syntax._
import io.onemsg.sdk.core.JsonSupport._

case class SendAddressMessageRequest(
  /* Recipient phone (E.164 digits, no +). Must match country. */
  phone: Option[Int] = None,
  chatId: Option[String] = None,
  /* Body text shown with the address request */
  body: String,
  /* Address form country. Defaults to IN if omitted. */
  country: Option[SendAddressMessageRequestEnums.Country] = None,
  /* Optional prefilled address fields */
  values: Option[Map[String, io.circe.Json]] = None,
  /* Optional previously saved addresses for the user */
  savedAddresses: Option[Seq[Map[String, io.circe.Json]]] = None,
  /* Optional field validation errors when re-prompting */
  validationErrors: Option[Map[String, io.circe.Json]] = None,
  quotedMsgId: Option[String] = None
)
object SendAddressMessageRequest {
  implicit val encoderSendAddressMessageRequest: Encoder[SendAddressMessageRequest] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        t.phone.map(v => "phone" -> v.asJson),
        t.chatId.map(v => "chatId" -> v.asJson),
        Some("body" -> t.body.asJson),
        t.country.map(v => "country" -> v.asJson),
        t.values.map(v => "values" -> v.asJson),
        t.savedAddresses.map(v => "saved_addresses" -> v.asJson),
        t.validationErrors.map(v => "validation_errors" -> v.asJson),
        t.quotedMsgId.map(v => "quotedMsgId" -> v.asJson)
      ).flatten
    }
  }
  implicit val decoderSendAddressMessageRequest: Decoder[SendAddressMessageRequest] = Decoder.instance { c =>
    for {
      phone <- c.downField("phone").as[Option[Int]]
      chatId <- c.downField("chatId").as[Option[String]]
      body <- c.downField("body").as[String]
      country <- c.downField("country").as[Option[SendAddressMessageRequestEnums.Country]]
      values <- c.downField("values").as[Option[Map[String, io.circe.Json]]]
      savedAddresses <- c.downField("saved_addresses").as[Option[Seq[Map[String, io.circe.Json]]]]
      validationErrors <- c.downField("validation_errors").as[Option[Map[String, io.circe.Json]]]
      quotedMsgId <- c.downField("quotedMsgId").as[Option[String]]
    } yield SendAddressMessageRequest(
      phone = phone,
      chatId = chatId,
      body = body,
      country = country,
      values = values,
      savedAddresses = savedAddresses,
      validationErrors = validationErrors,
      quotedMsgId = quotedMsgId
    )
  }
}

object SendAddressMessageRequestEnums {

  type Country = Country.Value
  object Country extends Enumeration {
    val IN = Value("IN")
    val SG = Value("SG")
  }

}
