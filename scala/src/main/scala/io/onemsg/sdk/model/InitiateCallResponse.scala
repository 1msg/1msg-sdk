/**
 * 1MSG WhatsApp Business API (Public)
 * Public client API for sending messages, managing groups, flows, and templates.  This is the API exposed to end customers via platform.1msg.io  **Authentication:** All requests require `token` query parameter with JWT or API key.  **Documentation:** https://help.1msg.io/platform-1msg/getting-start/quick-start **API Docs:** https://docs.1msg.io/ 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@1msg.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package io.onemsg.sdk.model

import io.circe.{Decoder, DecodingFailure, Encoder, Json}
import io.circe.syntax._
import io.onemsg.sdk.core.JsonSupport._

  /**
   * Successful call action response from upstream, returned as-is. On empty upstream body, 1msg may return `{ \"result\": \"success\" }`. Upstream failures are often wrapped as HTTP 200 `{ \"response\": { \"error\": \"...\" } }`. 
   */
case class InitiateCallResponse(
  messagingProduct: Option[String] = None,
  success: Option[Boolean] = None,
  /* Present on outbound `connect` — contains the new call id */
  calls: Option[Seq[InitiateCallResponseCallsInner]] = None,
  /* Legacy success marker when upstream returns an empty body */
  result: Option[String] = None,
  response: Option[InitiateCallResponseResponse] = None
)
object InitiateCallResponse {
  implicit val encoderInitiateCallResponse: Encoder[InitiateCallResponse] = Encoder.instance { t =>
    Json.fromFields{
      Seq(
        t.messagingProduct.map(v => "messaging_product" -> v.asJson),
        t.success.map(v => "success" -> v.asJson),
        t.calls.map(v => "calls" -> v.asJson),
        t.result.map(v => "result" -> v.asJson),
        t.response.map(v => "response" -> v.asJson)
      ).flatten
    }
  }
  implicit val decoderInitiateCallResponse: Decoder[InitiateCallResponse] = Decoder.instance { c =>
    for {
      messagingProduct <- c.downField("messaging_product").as[Option[String]]
      success <- c.downField("success").as[Option[Boolean]]
      calls <- c.downField("calls").as[Option[Seq[InitiateCallResponseCallsInner]]]
      result <- c.downField("result").as[Option[String]]
      response <- c.downField("response").as[Option[InitiateCallResponseResponse]]
    } yield InitiateCallResponse(
      messagingProduct = messagingProduct,
      success = success,
      calls = calls,
      result = result,
      response = response
    )
  }
}

